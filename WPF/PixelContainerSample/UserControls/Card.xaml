<UserControl
    x:Class="PixelContainerSample.UserControls.Card"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PixelContainerSample.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

        <Border x:Name="LayoutRoot" Background="White" CornerRadius="30">
            <StackPanel Margin="20">
                <TextBlock Text="CHARACTER'S NAME"></TextBlock>
                <TextBlock Text="{Binding CharacterName}" FontSize="20"></TextBlock>
                <TextBlock Text="CAPACITY"></TextBlock>
                <TextBlock Text="{Binding Strength}" FontSize="20"></TextBlock>
                <TextBlock Text="GOOD / BAD"></TextBlock>
                <TextBlock Text="{Binding GoodOrBadGuy}" FontSize="20"></TextBlock>
            </StackPanel>
        </Border>
</UserControl>
